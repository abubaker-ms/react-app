import { render,screen } from "@testing-library/react";
import ResturantMenu from "../ResturantMenu";
// import MOCK_DATA from '../mocks/resturantMenuMockData.json'
import { ResturantConatiner } from "../RestuarntConatiner";
import useResturantMenu from "../../../utils/useResturantMenu";
import "@testing-library/jest-dom";


it("Should load resturant card data",()=>{

    // render(<ResturantConatiner resid={MOCK_DATA}/>)

    // const resInfo=screen.getByText("₹300 for two")

    // expect(resInfo).toBeInTheDocument();
})
